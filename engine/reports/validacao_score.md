# Validação score v2

Amostra de produção ainda não carregada neste repositório (n=0 no store vazio).

Não se publica correlação Pearson/Spearman até `etl all` + `marketplace aggregate` + `scores_imobiliario` terem n≥8.

Comando: `python -m convergeo_engine.cli scoring-validate`
